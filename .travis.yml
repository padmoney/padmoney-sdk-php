language: php
sudo: false
dist: trusty
php:
  - 5.6
  - 7.1
  - 7.2
  - 7.3
  - 7.4
  - nightly
before_install:
  - travis_retry composer self-update
  - travis_retry composer update --no-interaction --prefer-source
cache:
  directories:
    - $HOME/.composer/cache
